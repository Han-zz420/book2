# 原始照片原图(约 2.5GB)不入库 —— 仅提交压缩后的 covers/
照片/

# 新导入的 532 张原始封面备份(213MB,不入库;压缩版在 covers/)
covers_full/

# 部署体积测试 / 打包产物
1/
cc/
cc.zip
cc (2).zip
*.zip

# 早期重复诊断脚本(已不用)
analyze.cjs

# 系统 / 临时文件
node_modules/
.DS_Store
Thumbs.db

# Workers/D1 本地开发状态与产物
backend/.wrangler/
backend/node_modules/
backend/package-lock.json
.wrangler/

# WorkBuddy 本地工作目录（内部日志/记忆，不入库）
.workbuddy/

# 本地临时/分析产物(不入库)
*.xlsx
_*.tsv
_*.py
_*.cjs
_*.json
